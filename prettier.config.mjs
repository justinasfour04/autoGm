/** @type {import("prettier").Config} */
const config = {
  plugins: ['prettier-plugin-tailwindcss'],
  singleQuote: true,
  trailingComma: "es5",
  requirePragma: false,
  arrowParens: "always"
}

export default config;